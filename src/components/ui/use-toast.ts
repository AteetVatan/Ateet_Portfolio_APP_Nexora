
import { useToast, toast } from "@/hooks/use-toast";

/**
 * Toast notification utilities
 * 
 * Re-exports the useToast hook and toast function from the hooks directory
 * to maintain compatibility with shadcn/ui structure while allowing for
 * future customization.
 */
export { useToast, toast };
